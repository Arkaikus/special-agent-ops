# special-agent-ops
a .cursor/.claude command that scans and evaluates a project and builds a specialized team of agents around it

## Available command specs

- `.cursor/commands/create-team.md` — `/create-team` command that discovers project stack, reconciles existing agents/skills, and scaffolds a specialized team plus `/team` manifest.
