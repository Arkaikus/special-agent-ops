# Generated agent: {{ agent_name }}

Built by `agentctl apply`. Run locally:

```bash
export {% if model.provider == "anthropic" %}{{ model.api_key_env }}{% elif model.provider == "ollama" %}OLLAMA_BASE_URL={{ model.base_url }}{% else %}{{ model.api_key_env or "OPENAI_API_KEY" }}{% endif %}
uv run uvicorn server:app --host 0.0.0.0 --port {{ port }}
```

Redis URL defaults to `redis://redis:6379/0` for Docker Compose.
