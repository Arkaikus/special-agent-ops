# swarm-rs scaffold: {{ agent_name }}

Milestone 1: `cargo build` and `/health` on port {{ port }}.

```bash
cd rust && cargo run
```

Milestone 2: add LLM providers behind feature flags (reqwest to Anthropic/OpenAI-compatible/Ollama).
