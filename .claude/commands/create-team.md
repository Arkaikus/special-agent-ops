---
name: create-team
description: Discover project stack and scaffold a specialized multi-agent team with a /team manifest
---

# /create-team

Use the same implementation contract as `.cursor/commands/create-team.md`.

When host-specific behavior differs, prefer `.claude/` paths and `.ref.md` skill references instead of symlinks.
