apiVersion: agentctl/v1
metadata:
  name: demo
spec:
  runtime: pydantic-ai
  model:
    type: ollama
    model_id: gemma4:e2b
    base_url: http://ollama:11434/v1
  prompts:
    system: You are a concise demo agent for agentctl.
  deploy:
    port: 8080
    composeService: demo
